import { Product } from './types';

// Inventário vazio para iniciar apenas com CSV
export const MOCK_INVENTORY: Product[] = [];

export const STANDARDIZED_LIST_SAMPLE = []; // Deprecated, using standardizedData.ts
